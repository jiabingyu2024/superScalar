{
  "$schema": "stage11_wave_debug",
  "version": "1.1",
  "design_name": "superScalar",
  "timestamp": "2026-07-11T11:30:00+08:00",
  "eda_tool": {
    "waveform_viewer": "Vivado Simulator waveform viewer",
    "waveform_version": "Vivado 2023.2, build 4029153",
    "dump_simulator": "xsim",
    "dump_format": "wdb",
    "dump_compile_flags": "xvlog --incr --relax; xelab --incr --debug typical --relax --mt 2",
    "dump_runtime_flags": "-key {Post-Implementation:sim_1:Functional:top} -tclbatch top.tcl",
    "actual_dump_command": "xsim post_impl_load_trace -tclbatch post_impl_load_trace.tcl -log post_impl_load_trace.log",
    "actual_wave_open_command": "open_wave_database top_func_impl.wdb",
    "source": "input_config.json and generated XSim scripts"
  },
  "failure_context": {
    "from_triage": "standalone board-failure analysis; stage10_debug_triage.json is absent",
    "failure_class": "data mismatch",
    "suspected_area": "srcWithMext RV32I dispatcher control flow and the DRAM-to-DCache refill data path"
  },
  "dump_status": {
    "dump_exists": false,
    "prior_dump_existed": true,
    "dump_format": "wdb",
    "dump_file": "/mnt/e/Resources/03_competitions/26_03_jcs/2607round/superScalar/fpga/build/digital_twin_srcWithMext/digital_twin.sim/sim_1/impl/func/xsim/top_func_impl.wdb (removed when the project was rebuilt; only fpga/diagnostics/post_impl_load_trace_evidence.log remains)",
    "rerun_required": true,
    "rerun_reason": "The available WDB/evidence predates the read_valid_d3 RTL and the 2026-07-11 10:33 bitstream. The checked-in diagnostic TB also stops after 120 cycles, before the previously observed cycle-2137 result, so it cannot currently reproduce the recorded evidence without correction.",
    "rerun_command": "Regenerate the post-implementation functional simulation from the current routed checkpoint, extend the diagnostic runtime beyond the final SEG write, then run fpga/diagnostics/run_post_impl_load_trace.bat"
  },
  "signals_traced": [
    {
      "signal": "SEG=0x33800000",
      "module": "srcWithMext software result protocol",
      "reason": "Decodes to RV32I 33/37 and M extension 8/8"
    },
    {
      "signal": "cpu_req_addr[1:0] -> cpu_resp_rdata",
      "module": "CoreDCache post-route netlist",
      "reason": "Post-route LUT network contains both address-low bits and the registered load-response mux"
    },
    {
      "signal": "req_byte_q[1:0]",
      "module": "CoreDCache post-route netlist",
      "reason": "Both miss-path offset registers exist and are driven by cpu_req_addr[1:0]"
    },
    {
      "signal": "mem_size[1:0], mem_signed",
      "module": "CoreMemIssueQueue and CoreExecuteCluster post-route netlist",
      "reason": "All fields are retained through the five-entry memory queue and drive load-extension LUTs"
    },
    {
      "signal": "entry_q payload primitives",
      "module": "CoreCompressedQueue post-route netlist",
      "reason": "Payload maps to FDRE cells with reset tied low; valid/count own visibility"
    },
    {
      "signal": "mem_req_addr, mem_resp_valid, mem_resp_rdata",
      "module": "CoreDCache post-route downstream port",
      "reason": "Pairs each refill word request with the value already presented to DCache, separating upstream DRAM alignment from DCache fill placement"
    }
  ],
  "observations": [
    "The diagnostic post-route simulation reproduces the board result by issuing an MMIO write of 0x33800000 to 0x80200020; it is not a stale-bitstream-only symptom.",
    "Dump-directed counter tracing shows SB, SH, and LB explicitly pass, then LH, LW, LBU, and LHU explicitly take their four fail paths; later tests continue. The dispatcher did not skip, repeat, or run away.",
    "At the CoreDCache downstream port, request 0x8010000c receives 0, request 0x80100010 receives 0x1234abcd, and request 0x80100014 receives 0x55667788. DRAM initialization places 0x1234abcd at 0x8010000c and 0x55667788 at 0x80100010, proving the response data trails the request by one word before entering DCache.",
    "LB passes because 0x80100008 is overwritten by the preceding SW test before LB executes; the overwritten cache word no longer depends on the corrupted refill value. LH/LW/LBU/LHU still consume the shifted initial line and fail.",
    "The cacheable range covers all 0x8010_xxxx test addresses. Refill and writeback requests reaching DramBramAdapter are word aligned; cacheable narrow-load lane selection is therefore owned by CoreDCache, not by the SoC adapter byte-write mask.",
    "The 100 printed Synth 8-7137 warnings plus one suppression message are all reported under CoreCompressedQueue. The post-route memory-queue payload registers have R=0 and are gated by valid/count ownership, so the warnings are real cleanup items but do not establish corruption of mem_size or mem_signed.",
    "The actual failing set is LH/LW/LBU/LHU. It is explained by the shifted initial cache line, whereas mem_size or mem_signed corruption cannot explain the correctly ordered pass/fail counter updates and the already-wrong downstream memory responses.",
    "Post-route timing meets constraints: WNS=0.274 ns, WHS=0.088 ns, TNS=0, THS=0.",
    "The first diagnosis was incomplete: changing resp_valid from read_valid_d2 to read_valid_d3 delays ownership but does not itself advance the registered BMG output.",
    "The 2026-07-11 build did synthesize E:/Resources/03_competitions/26_03_jcs/2607round/superScalar/rtl/soc/DramBramAdapter.sv with read_valid_d3, and produced top.bit at 10:33. Therefore the repository fix was present in that implementation; it was not omitted by synthesis.",
    "The generated BMG model has C_HAS_MEM_OUTPUT_REGS_A=1, C_HAS_ENA=1 and C_HAS_REGCEA=0. Its output-stage regce_i is therefore ENA. DramBramAdapter connects DRAM_0.ena only to req_valid, while CoreDCache emits a one-cycle refill request and waits. The request edge loads the addressed word into the internal memory output, but the following edge has ENA=0, so the primitive output register cannot transfer that word to douta. The next request pulse transfers the preceding word, exactly matching the observed one-word lag.",
    "rtl/ip/DRAM_0.sv does not faithfully model that enable contract: douta <= read_data_d1 executes even when ena=0. This lets the Verilator model advance on the idle second edge and masks the FPGA failure."
  ],
  "root_cause": {
    "found": true,
    "description": "DramBramAdapter pulses DRAM_0.ena for only the request edge even though the selected BMG primitive output register uses the same ENA as its register clock enable. The target word reaches the internal memory output but cannot reach douta on the idle following edge; the next request pulse exposes the preceding word. Delaying resp_valid to read_valid_d3 leaves this enable/data-advance bug intact.",
    "evidence": "The old post-route trace shows a repeatable one-word lag. The current generated DRAM_0 model independently establishes the mechanism: C_HAS_MEM_OUTPUT_REGS_A=1, C_HAS_ENA=1, C_HAS_REGCEA=0, regce_i=(C_HAS_REGCE==0)&&(C_HAS_EN==0||EN), and both the memory read and output register are edge-triggered. The latest synthesis log confirms the d3 adapter was used, while the board still reports 0x33800000.",
    "affected_module": "rtl/soc/DramBramAdapter.sv DRAM_0.ena generation, plus rtl/ip/DRAM_0.sv enable/latency modeling"
  },
  "fix_recommendation": {
    "type": "rtl_and_ip_contract",
    "description": "Keep byte-write masking and the primitive output register unchanged. Enable the independent BMG REGCEA port: use ENA on the request edge to load the memory-output latch, use read_valid_d1 as REGCEA on the following edge to transfer the latch to DOUT, and align response valid/offset at d2. Update the behavioral model to separate ENA-controlled latch update from REGCEA-controlled DOUT update. Verify with isolated and consecutive non-zero words in current post-implementation simulation before generating the next bitstream.",
    "target_files": [
      "rtl/soc/DramBramAdapter.sv",
      "rtl/ip/DRAM_0.sv",
      "fpga/create_vivado_project.tcl",
      "docs/fpga/ip_timing_alignment.md"
    ]
  },
  "fix_implementation_status": {
    "state": "implemented_in_source_pending_reverification",
    "timestamp": "2026-07-11T11:08:00+08:00",
    "changes": [
      "fpga/create_vivado_project.tcl enables Use_REGCEA_Pin for DRAM_0",
      "rtl/soc/DramBramAdapter.sv drives REGCEA from read_valid_d1 and aligns response ownership at d2",
      "rtl/ip/DRAM_0.sv models separate ENA latch and REGCEA output-register phases"
    ],
    "not_yet_done": "DRAM_0 regeneration, Vivado synthesis/implementation, current post-implementation trace, and board confirmation"
  },
  "additional_debug_sessions": [
    {
      "timestamp": "2026-07-11T12:35:58+08:00",
      "name": "p1b_int_rrd_early_wakeup_data_mismatch",
      "eda_tool": {
        "waveform_viewer": "GTKWave 3.3.116 available but GUI not opened because DISPLAY is unavailable; FST was inspected through fst2vcd and a signal-selective parser",
        "waveform_version": "GTKWave package 3.3.116-1build2 / fst2vcd",
        "dump_simulator": "Verilator 5.046",
        "dump_format": "fst",
        "dump_compile_flags": "--trace-fst",
        "dump_runtime_flags": "--trace --wave=build/wave/rv32/rv32ui-p-st_ld.fst --max-cycles=400",
        "actual_dump_command": "python3 scripts/run_verilator.py rv32 --test rv32ui-p-st_ld --no-build --trace --max-cycles 400",
        "actual_wave_open_command": "fst2vcd -f build/wave/rv32/rv32ui-p-st_ld.fst -o /tmp/p1b_st_ld.vcd; GUI alternative: gtkwave build/wave/rv32/rv32ui-p-st_ld.fst",
        "source": "standalone detection from scripts/run_verilator.py and PATH; stage8_eda_env.json is absent"
      },
      "failure_context": {
        "from_triage": "standalone P1B regression analysis; stage10_debug_triage.json is absent",
        "failure_class": "data mismatch after adding INT IQ-to-RRD stage and early wakeup",
        "suspected_area": "producer PRF write versus dependent MEM IQ issue/AGU capture timing"
      },
      "dump_status": {
        "dump_exists": true,
        "dump_format": "fst",
        "dump_file": "build/wave/rv32/rv32ui-p-st_ld.fst",
        "rerun_required": true,
        "rerun_command": "Rebuild after restricting early wakeup to the INT IQ, then rerun rv32ui-p-st_ld with --trace"
      },
      "signals_traced": [
        {
          "signal": "int_rrd_valid_q, int_rrd_uop_q.prd, int_early_wakeup_valid/prd",
          "module": "CoreExecuteCluster / CoreBackend",
          "reason": "Identify the fixed-latency producer and the cycle its dependency tag is broadcast"
        },
        {
          "signal": "mem_issue_valid, mem_issue_fire, prf_raddr[4:5], prf_rdata[4:5]",
          "module": "CoreMemIssueQueue / CoreExecuteCluster",
          "reason": "Determine whether a dependent MEM uop reads the PRF before the producer write edge"
        },
        {
          "signal": "prf_we[1:0], prf_waddr[1:0], prf_wdata[1:0], mem_req_addr_q",
          "module": "CorePhysRegFile / CoreExecuteCluster",
          "reason": "Pair the producer's edge write with the address value captured by the dependent AGU"
        }
      ],
      "observations": [
        "The initial packed exec_complete wakeup implementation produced UNOPTFLAT through MEM IQ -> PRF -> exec_complete vector -> wakeup -> MEM IQ and failed rv32ui-p-st_ld; direct RRD metadata removed UNOPTFLAT but the same functional failure remained.",
        "At FST timestamp 1950 ps, INT early-wakeup valid is 2'b11 for PRD 27 and 28 while a dependent MEM uop fires with PRF read addresses 27 and 28. Its read data is still 0/0, while the same edge writes PRD27=0x80002000 and PRD28=0xffffffdd. The MEM request therefore captures operands one edge too early.",
        "The pattern repeats at 2470 ps: a MEM uop reads PRD20 value 0x00000009 in the same edge the INT producer writes PRD20=0x8000103c. At 2770 ps, MEM reads stale PRD20/PRD7 values 8/8 while those producer tags are written with the fail signature/address chain.",
        "An INT consumer is safe to use this early wakeup because it is captured into its own RRD register and reads the PRF after the producer edge. MEM and MUL consumers have no equivalent RRD operand boundary and consume PRF data in the wakeup cycle."
      ],
      "root_cause": {
        "found": true,
        "description": "P1B broadcast the INT producer's RRD early-wakeup tag to all issue queues. A dependent MEM/MUL uop can issue immediately and read/capture stale PRF operands in the same edge that the INT producer writes its result. Early wakeup is only timing-correct for the newly registered INT consumer path.",
        "evidence": "rv32ui-p-st_ld writes tohost=0x0000053d at cycle 279. The FST shows exact tag equality between early producer PRDs and dependent MEM PRF addresses, stale pre-edge PRF data, and the correct producer data arriving on the same write edge.",
        "affected_module": "rtl/core/backend/CoreBackend.sv wakeup routing; MEM/MUL queue inputs must not use INT RRD early wakeup without operand bypass or their own RRD stage"
      },
      "fix_recommendation": {
        "type": "rtl_fix",
        "description": "Feed mixed INT-early plus registered MEM/MUL wakeup only to CoreIntIssueQueue. Restore complete_valid/complete_prd for CoreMemIssueQueue and CoreMulDivIssueQueue. Do not add an EXE-to-AGU combinational bypass in this phase; a future MEM RRD/operand queue may safely reintroduce earlier wakeup.",
        "target_files": [
          "rtl/core/backend/CoreBackend.sv"
        ]
      },
      "rerun_path": "frontend-compile-sim-run"
    }
  ],
  "rerun_path": "frontend-compile-sim-run"
}
