{
  "timestamp": "2026-07-09T12:45:00+08:00",
  "trigger": "srcWithMext Verilator PASS but board still reports wrong SEG/fail symbol; static SoC CDC review",
  "backup_dir": "backup/20260709_124500_static_soc_review",
  "files_modified": [
    {
      "path": "rtl/soc/DramBramAdapter.sv",
      "change_type": "logic_fix",
      "notes": "Restored DRAM read response and byte-offset alignment to one cycle after checking the generated Vivado wrapper reports READ_LATENCY=1."
    },
    {
      "path": "fpga/create_vivado_project.tcl",
      "change_type": "ip_config_and_constraint_fix",
      "notes": "Set DRAM_0 Register_PortA_Output_of_Memory_Primitives=false and generate a late CDC XDC that groups clk_out1_pll and clk_out2_pll asynchronously."
    },
    {
      "path": "rtl/soc/counter.sv",
      "change_type": "cdc_fix",
      "notes": "Split counter reset into cpu_rst and cnt_rst so each clock domain uses a reset synchronized to that domain; marked CDC synchronizer stages with ASYNC_REG."
    },
    {
      "path": "rtl/soc/top.sv",
      "change_type": "cdc_fix",
      "notes": "Added a 50 MHz synchronized mirror for slow status signals feeding twin_controller; later static review found the dynamically scanned SEG bus must not be crossed this way."
    },
    {
      "path": "rtl/soc/student_top.sv",
      "change_type": "cdc_annotation",
      "notes": "Marked reset, virtual switch, and key synchronizer stages with ASYNC_REG."
    },
    {
      "path": "rtl/soc/SocMemBridge.sv",
      "change_type": "cdc_fix",
      "notes": "Connected counter CPU-side reset to the CPU domain reset and counter-side reset to the 50 MHz counter domain reset; moved SEG scan generation to the 50 MHz domain after synchronizing the raw seg_wdata register, avoiding bitwise CDC of the dynamic 40-bit scanned SEG bus."
    },
    {
      "path": "rtl/soc/perip_bridge.sv",
      "change_type": "interface_update",
      "notes": "Updated the legacy counter instantiation to the split-reset interface to avoid future recursive-compile port mismatches."
    },
    {
      "path": "docs/fpga/ip_timing_alignment.md",
      "change_type": "documentation",
      "notes": "Corrected the DRAM timing contract to one-cycle READ_LATENCY and documented that generated C_READ_LATENCY_A is the source of truth."
    },
    {
      "path": "docs/rtl_changes.json",
      "change_type": "documentation",
      "notes": "Recorded the second board-debug pass and backup location."
    }
  ],
  "cascade_scope": "compile_and_lint_and_cdc_check",
  "verification": [
    "Static review: generated fpga/build/.../DRAM_0.v reports X_INTERFACE READ_LATENCY 1 and C_READ_LATENCY_A=1.",
    "Static review: generated IROM_0 also reports READ_LATENCY 1, matching the current fetch-side assumption.",
    "Static review: existing fpga/build/digital_twin_srcWithMext/pll.xci still shows CLKOUT2_REQUESTED_OUT_FREQ=80.000, so that build is stale or was generated with FPGA_CPU_CLK_MHZ=80 rather than the current Tcl default of 50.",
    "Static review: existing routed timing report says Timing constraints are not met; clk_out2_pll -> clk_out1_pll has WNS=-0.131ns/TNS=-0.750ns across 9 failing endpoints.",
    "Static review after srcWithMext Verilator PASS: top-level still crossed the continuously changing 40-bit scanned SEG bus from CPU clock to 50 MHz for twin_controller. This can produce incoherent segment snapshots on FPGA even when the CPU program passes."
  ],
  "notes": "Current evidence points to four board-only risks: the previous two-cycle DRAM adapter change was not aligned with the actual generated Vivado READ_LATENCY=1, the existing build directory does not reflect the current default CPU clock, the old routed build failed timing on a CPU-to-50MHz crossing, and the 40-bit scanned SEG bus was being sampled across clock domains by twin_controller. If LED/SEG still fail after a clean rebuild, add a temporary hardware-visible heartbeat for pll.locked, CPU reset release, irom_ena, and first MMIO LED write before changing CPU logic."
}
